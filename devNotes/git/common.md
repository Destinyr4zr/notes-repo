###### standard scenario
```Shell
git init
git add .
git commit -m 'message'
#automatic login
git remote add origin https://github.com/Destinyr4zr/notes-repo.git
#or git remote add origin https://[TOKEN]@github.com/[USER]/[REPO]
git branch #something
git config credential.helper store
git push -u origin main
```
